createMask & new_yellow_mask function: filter out all colors except for yellow, which is the color of the duckie

get_duckie_position(camera): input camera object, output duckie position in camera frame

get_image_data(image): input image taken by camera, output mapping of duckie onto the camera frame